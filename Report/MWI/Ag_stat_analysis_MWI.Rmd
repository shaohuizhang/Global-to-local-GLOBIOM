---
title: 'Analysis of agricultural statistics MWI'
author: "Michiel van Dijk"
date: "`r format(Sys.time(), '%B %d, %Y')`"
output: pdf_document
classoption: landscape
---

```{r setup, include=FALSE}
library(rprojroot)
root <- find_root(is_rstudio_project)

library(knitr)
knitr::opts_chunk$set(
  fig.width=12, fig.height=8,
  dpi = 300,
  echo=FALSE, warning=FALSE, message=FALSE,
  fig.path = file.path(root,"FigTabMap/out-"),
  dev = "CairoPNG",
  dev.args = list(CairoPNG = list(bg = "transparent"))
  )

library(pander)
panderOptions('table.alignment.default', function(df)
    ifelse(sapply(df, is.numeric), 'right', 'left'))
panderOptions('table.split.table', Inf)
panderOptions('big.mark', ",")
panderOptions('keep.trailing.zeros', TRUE)

source(file.path(root, "Code/MWI/Agricultural_statistics/Analyse_ag_stat_MWI.R"))
```

```{r area_rank}
pander(area_rank_FAOSTAT)
```

\pagebreak
```{r area_rank_nat}
pander(area_rank_nat)
```

\pagebreak
```{r area_compare}
area_compare
```

\pagebreak
```{r prod_compare}
prod_compare
```

\pagebreak
```{r yld_compare}
yld_compare
```

\pagebreak
```{r heads_compare}
heads_compare
```

\pagebreak
```{r area_share_within_reg}
pander(area_share_within_reg)
```

\pagebreak
```{r Fig_area_share}
Fig_area_share
```

\pagebreak
```{r yld_outlier}
yld_outlier
```

\pagebreak
```{r yld_winsor}
yld_winsor
```

